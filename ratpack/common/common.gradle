apply plugin: 'java'

dependencies {
  compile "io.ratpack:ratpack-guice:${versions.ratpack}"
  compile ('com.zaxxer:HikariCP:1.2.8') {
    exclude module: 'javassist'
    exclude module: 'slf4j-api'
  }
}